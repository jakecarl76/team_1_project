<%- include('../includes/head.ejs') %>
<body class="content-body">
   <header>
      <%- include('../includes/nav.ejs') %>
   </header>
   <main class="content-main">
      <h1 class="content-main-header">Hermit Habitat's Movies</h1>

      <% if (content) { %>
      <div class="content-cards">
      <% for (let item of content) { %>
            <div class="content-card">
               <div class="left">
               <img class="content-card-image" src="<%= item.imageUrl %>" alt="<%= item.title %>">
               </div>
               <div class="right">
               <h2><%= item.title %></h2>
               <h3>Rating: <%= item.rating %></h3>
               <h4>Genre: <%= item.genre %></h4> 
               <h5 class="description">Description: <%= item.description %></h5>
               </div>
            </div>
         <% } %> 
      </div>
      <% } else { %>
         <img class="hermit-nothing" src="images/hermit.png" alt="Hermit Crab">
         <h2>Whoops! Looks like we have nothing here!</h2>
      <% } %> 
   </main>
   <!-- Trying a read more/see more option -->
   <!-- <script src="js/scripts.js"></script> -->
   <%- include('../includes/end.ejs') %>
</body>
</html>